<div class="container">
  <div class="row">
    <div class="col-xs-12 col-md-6 col-md-offset-3">
      <div class="text-center">
        <h1><%= @item.name %> </h1> <br>
        <p><%= @item.description %> </p><br>
        <h3><%= humanized_money_with_symbol(@item.price) %> </h3>  <br>
        <h4> <%= @item.quantity %> available</h4>
      </div>
    </div>
  </div>
</div>
